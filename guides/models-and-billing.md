# 模型与计费

> **数据来源与核验**
> - **来源**：[{{SITE_NAME}}「模型广场」]({{SITE_URL}}/#pricing) 与登录后的[控制台「订阅套餐」]({{SITE_URL}}/subscription)
> - **核验范围**：模型列表、模型 ID 与单价，订阅套餐的价格与每日额度，以及「订阅额度用尽后自动改按余额计费」开关
> - **最后核验日期**：2026 年 9 月 6 日

本页说明模型单价的公示位置、按量计费与订阅套餐两种计费方式，以及预扣结算与缓存计费规则。模型可用性、单价和套餐可能调整，使用前以站点实时显示为准

## 模型广场

模型单价由 **模型广场** 公示；登录后还可查看平台提供的实时可用率信息

![模型广场](/assets/images/models-and-billing/model-marketplace.png)

> **注意**
> 上图为 2026 年 9 月 6 日的界面留档，可能包含当前已不再展示的模型，当前模型列表以模型广场实时显示为准

模型广场中的单价单位为美元／百万 Token。各类 Token 费用按「该类计费用量 ÷ 1,000,000 × 对应单价」计算，输入、输出、缓存读取和缓存写入分别计价。**实际扣费 = 各类 Token 费用之和 × 令牌所在分组的计费倍率**，各分组的倍率见[分组与计费倍率](/guides/access.md#分组与计费倍率)。最终扣费结果以控制台 **额度流水** 为准

## 计费方式

**按量计费**：按实际 Token 用量从账户余额扣除。根据核验时的页面说明，账户余额长期有效；如规则调整，以充值页和控制台说明为准

**订阅套餐**：订阅期内按窗口额度调用，不消耗账户余额

![订阅套餐](/assets/images/models-and-billing/subscription-plans.png)

> **注意**
> 套餐额度每日重置，并同时展示每周和每月总额度。具体恢复时间、统计窗口和未使用额度的处理方式，以登录后的订阅页为准

订阅页提供「订阅额度用尽后自动改按余额计费」开关，**默认关闭**。关闭状态下额度用尽即暂停调用，并在响应中给出恢复时间；开启后额度用尽自动转为扣减余额

## 预扣与额度流水

**额度流水** 记录账户额度变动，页面可能显示预扣、预扣退回和消费等类型

调用发起后，系统会先预扣 $1；调用结束后按实际用量结算，并退回未使用部分。单次调用的完整记录形式如下：

|时间|类型|变动|
|---|---|---|
|08:14:32|预扣|-$1.000000|
|08:14:33|预扣退回|+$0.668404|

上例中，两笔流水的净扣减额为 $0.331596。额度流水中的请求 ID 可与 **使用日志** 中的具体调用对应，用于核对用量和扣费

> **注意**
> 可用余额低于 $1 时可能无法发起调用，即使本次请求的最终费用低于 $1。规则如有调整，以控制台返回信息为准

## 缓存计费

模型广场中的「缓存读取」价格为对应输入价格的十分之一。部分客户端和模型支持复用上下文缓存；命中缓存时，相关输入会按缓存读取价格计费

缓存行为取决于客户端、模型和上游实现，不能保证每次请求都命中。同一任务保持在连续会话中，通常更有利于复用已有上下文
