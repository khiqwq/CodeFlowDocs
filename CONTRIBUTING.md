# 贡献指南

## 文件与导航

- 每篇指南使用一个 `guides/<slug>.md` 文件；文件名只使用小写英文字母、数字和连字符
- 新文件必须同时登记到 `manifest.json`。`manifest.json` 是站内导航的唯一来源，分组和条目顺序就是展示顺序
- `slug` 必须匹配 `^[a-z0-9]+(-[a-z0-9]+)*$`，且对应文件必须存在，不能有未登记的孤儿文件
- 图片统一放在 `assets/`，文件名使用有意义的英文名称

## Markdown 写作规范

- 正文必须以唯一的 `# 主标题` 开头，标题层级不超过 h4
- 只使用纯 GFM：标题、段落、列表、表格、围栏代码块、行内代码、引用、粗斜体、分隔线和页内锚点链接
- 禁止 frontmatter、VitePress 容器、Vue 组件、原生 HTML，以及 GitHub 告示语法。提示和警告使用引用块，例如：

  ```markdown
  > **配置时效性**
  > 客户端界面可能随版本更新变化，以实际界面为准
  ```

- 段落、列表项、表格单元格和引用行的末尾不写句号，块内句子之间的标点照常使用
- 指向其他指南时使用 `/guides/<slug>.md` 根绝对路径链接；外部链接使用完整 `https://` 地址
- 锚点由标题文本生成：小写、去标点、保留中日韩字符、空白转 `-`，例如 `## 一键配置 Claude Code 与 Codex` 对应 `#一键配置-claude-code-与-codex`；不要书写 `{#…}` 自定义锚点。同一篇内各标题生成的锚点必须唯一，且去除标点后不得为空
- 站点地址和站点名使用 `{{SITE_URL}}` 与 `{{SITE_NAME}}`，第三方地址保持原样
- 图片使用根绝对路径引用（`/assets/…`），只能引用 `assets/` 下的 png、jpg、jpeg、webp 或 gif，单文件不超过 5 MB，禁止外链图片和 SVG
- 示例令牌统一使用 `sk-您的令牌`，不得提交真实令牌。截图、日志和示例配置也必须脱敏

## 动态数据

模型、价格、套餐、充值规则和分组倍率可能变化。修改这些内容前，应实际核对 CodeFlow 控制台或公开来源，并在正文中更新来源、核验范围和核验日期；不要使用编辑日期代替核验日期

## 提交前检查

在仓库根目录运行 `node scripts/check.mjs`，全部通过后再提交；push 与 PR 时 CI 会自动执行同一脚本

确认 `manifest.json` 合法、所有条目和图片均有对应文件，指南无禁止语法和硬编码站点地址，并在 GitHub 网页端抽查 Markdown 预览
